# 后端联调地址(已废弃路径):App 现已独立运行,默认不连后端。仅 web 联调时可填。
EXPO_PUBLIC_API_URL=http://localhost:8000

# ===== AI 直连(DeepSeek,App 独立运行的核心)=====
# EXPO_PUBLIC_* 在打包时静态内联进 bundle(非运行期读取)——改 key 必须重新 build。
# 本地开发:把真 key 写进 apps/mobile/.env(已 gitignore,勿提交)。
# EAS 云端 build:key 用 `eas secret:create --name EXPO_PUBLIC_AI_API_KEY --value sk-xxx`
#   注入(不要把真 key 写进会提交的 eas.json)。base_url / model 已在 eas.json 的 env 里给默认值。
EXPO_PUBLIC_AI_API_KEY=
EXPO_PUBLIC_AI_BASE_URL=https://api.deepseek.com
EXPO_PUBLIC_AI_MODEL=deepseek-chat

# Supabase(云端阶段预留,当前未用)
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
